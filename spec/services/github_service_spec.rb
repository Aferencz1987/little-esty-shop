require 'rails_helper'
RSpec.describe GithubService do
  it "returns user data" do

    # mock_response = '{ncjsdil}'
    # allow(Faraday::Response).to receive(:body).and_return(mock_response)
    # json = GithubService.user_info('Aferencz1987')

    # expect(json).to be_a(Hash)
    # expect(json).to have_key(:followers)
    # expect(json).to have_key(:following)
    # expect(json).to have_key(:name)
    # expect(json).to have_key(:login)
    # expect(json).to have_key(:created_at)
  end
end
